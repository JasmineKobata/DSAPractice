class Solution {
    public int minEatingSpeed(int[] piles, int h) {
        int start = 1, end = nums.length-1;
        while (start < end) {
            int mid = start + (end - start) / 2;
        }
    }
} 